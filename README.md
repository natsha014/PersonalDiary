# 📔 Личный дневник (Django MTV)

Веб-приложение для ведения личных записей. Позволяет пользователям регистрироваться, создавать заметки с картинками,
редактировать их и искать нужную информацию в своих архивах.

## ✨ Основной функционал

- **Регистрация и вход:** Каждый пользователь имеет своё закрытое пространство.
- **Безопасность:** Доступ к заметкам (просмотр, правка, удаление) имеет только автор.
- **CRUD записей:** Полноценное управление заметками (Заголовок, Содержимое, Превью-изображение).
- **Поиск:** Быстрый поиск по заголовкам и тексту заметок.
- **Дизайн:** Адаптивная верстка на Bootstrap 5.

## 🛠 Технологии

- **Python 3.13 / Django** (MTV архитектура)
- **PostgreSQL** (База данных)
- **Docker & Docker Compose** (Контейнеризация)
- **Nginx** (Раздача статики и проксирование)
- **Pytest/Django Test** (Покрытие тестами основных функций)

---

## 🚀 Запуск проекта (Docker)

### 1. Подготовка

Создайте файл `.env` в корне проекта и заполните его на основе `.env.sample`:

### 2. Сборка и запуск

Выполните команду:

```bash
  docker-compose up -d --build
```

Проект будет доступен по адресу: http://localhost:82

## 🌍 Деплой на сервере

Проект развернут на удаленном сервере.
Адрес сайта: http://81.26.180.221:82

Админка: `/admin/` (используется для управления пользователями и записями)

## 🧪 Тестирование

Для запуска тестов выполните:

```bash
    python manage.py test
```

**Тесты проверяют:**

- Права доступа (защита чужих записей).
- Корректность создания и редактирования заметок.
- Работу поискового фильтра.

**📂 Структура проекта:**

- config/ — настройки проекта.
- diary/ — основное приложение (модели, шаблоны, логика дневника).
- users/ — управление пользователями и кастомная модель User.
- nginx/ — конфигурация веб-сервера.
- static/ — локальные файлы стилей и скриптов.
